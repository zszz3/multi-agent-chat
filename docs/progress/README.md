# Progress Docs

This directory stores time-scoped progress notes, refactor logs, and branch-era implementation status documents.

## Current Docs

- `2026-06-28-renderer-refactor-progress.md`: renderer shell decomposition and feature-boundary refactor progress

## Rule

If a document mainly captures phase progress, branch status, or migration checkpoints instead of stable architecture truth, place it here instead of the `docs/` root.
