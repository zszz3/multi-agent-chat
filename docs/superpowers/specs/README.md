# Superpowers Specs

Design specs live here after the design has been reviewed and accepted.

## Topic Areas

- `runtime/`: runtime contracts, phased architecture work, routing, driver boundaries, and related proof documents
- `workflow/`: workflow product, UX, and execution-surface design docs

Choose the narrowest topic folder that matches the change. Avoid adding flat files directly under `specs/`.
